/**
 * components/index.ts - 컴포넌트 통합 export
 * 
 * 🚧 현재 구현 단계: 기본 구조
 * 📅 다음 확장 예정: 실제 컴포넌트들 export
 * 📊 복잡도: ⭐ (입문)
 * 
 * 💡 사용 예시 (미래):
 * ```typescript
 * import { MapContainer, Button } from '@/components';
 * ```
 */

// 🚧 현재는 빈 파일 - 컴포넌트 추가 시 여기서 export

// 미래 예시:
// export { MapContainer } from './map/MapContainer';
// export { Button } from './common/Button';
// export { Modal } from './common/Modal';

export {}; // 빈 export로 TypeScript 오류 방지